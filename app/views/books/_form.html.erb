<!-- Book Search -->
<% if book.new_record? %>
  <div data-controller="book-search" data-book-search-url-value="<%= api_v1_book_search_index_path %>" data-book-search-editions-url-value="<%= editions_api_v1_book_search_index_path %>" data-book-search-amazon-tag-value="<%= amazon_affiliate_tag %>" data-book-search-mode-value="new" class="mb-8">
    <div class="relative">
      <label class="block text-sm font-medium text-gray-700 mb-1">Search for a book</label>
      <div class="relative">
        <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-3">
          <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
        </div>
        <input type="text"
               data-book-search-target="input"
               data-action="input->book-search#search"
               class="block w-full rounded-lg border-gray-300 pl-10 pr-4 py-3 text-base shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
               placeholder="Search by title, author, or ISBN...">
      </div>

      <!-- Search Results Dropdown -->
      <div data-book-search-target="results"
           class="hidden absolute z-20 mt-1 w-full bg-white rounded-lg shadow-lg border border-gray-200 max-h-96 overflow-y-auto">
      </div>
    </div>

    <!-- Divider -->
    <div class="relative mt-6">
      <div class="absolute inset-0 flex items-center">
        <div class="w-full border-t border-gray-200"></div>
      </div>
      <div class="relative flex justify-center text-sm">
        <span class="bg-white px-4 text-gray-500">or enter details manually</span>
      </div>
    </div>
  </div>
<% else %>
  <!-- Edition switcher for existing books -->
  <div data-controller="book-search" data-book-search-url-value="<%= api_v1_book_search_index_path %>" data-book-search-editions-url-value="<%= editions_api_v1_book_search_index_path %>" data-book-search-amazon-tag-value="<%= amazon_affiliate_tag %>" data-book-search-mode-value="edit" class="mb-6">
    <!-- Change Edition button -->
    <div data-book-search-target="editToggle">
      <button type="button" data-action="book-search#showEditSearch" class="inline-flex items-center gap-2 text-sm font-medium text-indigo-600 hover:text-indigo-500 transition-colors">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
        </svg>
        Change edition
      </button>
    </div>

    <!-- Edition search panel (hidden until "Change edition" is clicked) -->
    <div data-book-search-target="editSearch" class="hidden">
      <!-- Search bar for finding different editions -->
      <div class="relative">
        <label class="block text-sm font-medium text-gray-700 mb-1">Search for a different edition</label>
        <div class="relative">
          <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-3">
            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
            </svg>
          </div>
          <input type="text"
                 data-book-search-target="input"
                 data-action="input->book-search#search"
                 class="block w-full rounded-lg border-gray-300 pl-10 pr-4 py-3 text-base shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                 placeholder="Search by title, author, or ISBN...">
        </div>
      </div>

      <!-- Editions dropdown (populated by JS) -->
      <div class="relative">
        <div data-book-search-target="results"
             class="hidden absolute z-20 mt-1 w-full bg-white rounded-lg shadow-lg border border-gray-200 max-h-96 overflow-y-auto">
        </div>
      </div>

      <button type="button" data-action="book-search#hideEditSearch" class="mt-2 text-sm text-gray-500 hover:text-gray-700 transition-colors">
        Cancel
      </button>
    </div>
  </div>
<% end %>

<%= form_with(model: book, class: "space-y-6") do |form| %>
  <% if book.errors.any? %>
    <div class="rounded-md bg-red-50 p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-red-800"><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h3>
          <div class="mt-2 text-sm text-red-700">
            <ul class="list-disc space-y-1 pl-5">
              <% book.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  <% end %>

  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
    <div class="sm:col-span-2">
      <%= form.label :title, class: "block text-sm font-medium text-gray-700" %>
      <%= form.text_field :title, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm", placeholder: "Enter book title" %>
    </div>

    <div class="sm:col-span-2">
      <%= form.label :author, class: "block text-sm font-medium text-gray-700" %>
      <%= form.text_field :author, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm", placeholder: "Enter author name" %>
    </div>

    <!-- Page Range Slider -->
    <div class="sm:col-span-2"
         data-controller="page-range-slider"
         data-page-range-slider-min-value="1"
         data-page-range-slider-max-value="<%= book.last_page || 0 %>"
         data-page-range-slider-current-min-value="<%= book.first_page || 1 %>"
         data-page-range-slider-current-max-value="<%= book.last_page || 0 %>"
         data-page-range-slider-recommended-min-value="0"
         data-page-range-slider-recommended-max-value="0">
      <label class="block text-sm font-medium text-gray-700 mb-2">Reading range</label>

      <!-- Number inputs -->
      <div class="flex items-center gap-3 mb-3">
        <div class="flex-1">
          <label for="book_first_page" class="block text-xs text-gray-500 mb-1">First page</label>
          <%= form.number_field :first_page, min: 1,
              data: { page_range_slider_target: "minInput", action: "input->page-range-slider#inputMin" },
              class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
              placeholder: "1" %>
        </div>
        <div class="flex items-center pt-5">
          <span class="text-gray-400">-</span>
        </div>
        <div class="flex-1">
          <label for="book_last_page" class="block text-xs text-gray-500 mb-1">Last page</label>
          <%= form.number_field :last_page, min: 1,
              data: { page_range_slider_target: "maxInput", action: "input->page-range-slider#inputMax" },
              class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
              placeholder: "Total pages" %>
        </div>
      </div>

      <!-- Slider track -->
      <div data-page-range-slider-target="track" class="relative h-6 select-none cursor-pointer">
        <!-- Background track -->
        <div class="absolute top-1/2 -translate-y-1/2 left-0 right-0 h-1.5 bg-gray-200 rounded-full"></div>

        <!-- Recommended range indicator -->
        <div data-page-range-slider-target="recommendedIndicator"
             class="hidden absolute top-1/2 -translate-y-1/2 h-1.5 bg-green-200 rounded-full opacity-60"></div>

        <!-- Active range highlight -->
        <div data-page-range-slider-target="rangeHighlight"
             class="absolute top-1/2 -translate-y-1/2 h-1.5 bg-indigo-500 rounded-full"></div>

        <!-- Min handle -->
        <div data-page-range-slider-target="minHandle"
             data-action="mousedown->page-range-slider#startDragMin touchstart->page-range-slider#startDragMin"
             class="absolute top-1/2 -translate-y-1/2 -translate-x-1/2 w-4 h-4 bg-white border-2 border-indigo-500 rounded-full shadow cursor-grab active:cursor-grabbing hover:scale-110 transition-transform"></div>

        <!-- Max handle -->
        <div data-page-range-slider-target="maxHandle"
             data-action="mousedown->page-range-slider#startDragMax touchstart->page-range-slider#startDragMax"
             class="absolute top-1/2 -translate-y-1/2 -translate-x-1/2 w-4 h-4 bg-white border-2 border-indigo-500 rounded-full shadow cursor-grab active:cursor-grabbing hover:scale-110 transition-transform"></div>
      </div>

      <p class="mt-1 text-xs text-gray-500">Drag handles to set your reading range (skip intros, appendices, etc.)</p>
    </div>

    <div>
      <%= form.label :density, class: "block text-sm font-medium text-gray-700" %>
      <%= form.select :density, Book.densities.keys.map { |d| [d.humanize, d] }, {}, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
      <p class="mt-1 text-xs text-gray-500">How dense is the material? Accounts for word count and complexity.</p>
    </div>

    <!-- Collapsible edition details (ISBN + cover URL) -->
    <% edition_details_filled = book.isbn.present? && book.cover_image_url.present? %>
    <div class="sm:col-span-2" data-controller="toggle" data-toggle-open-value="<%= !edition_details_filled %>">
      <button type="button" data-action="toggle#toggle" class="inline-flex items-center gap-1.5 text-sm font-medium text-gray-600 hover:text-gray-900 transition-colors">
        <svg data-toggle-target="icon" class="w-4 h-4 transition-transform <%= 'rotate-90' unless edition_details_filled %>" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
        Edition details
      </button>
      <div data-toggle-target="content" class="<%= 'hidden' if edition_details_filled %> mt-3 grid grid-cols-1 gap-6 sm:grid-cols-2">
        <div>
          <%= form.label :isbn, "ISBN (optional)", class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :isbn, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm", placeholder: "ISBN-10 or ISBN-13" %>
        </div>

        <div class="sm:col-span-2">
          <%= form.label :cover_image_url, "Cover image URL (optional)", class: "block text-sm font-medium text-gray-700" %>
          <%= form.url_field :cover_image_url, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm", placeholder: "https://..." %>
        </div>
      </div>
    </div>
  </div>

  <% if book.new_record? %>
    <div class="relative flex items-start mt-2">
      <div class="flex h-6 items-center">
        <%= check_box_tag :add_to_reading_list, "1", true, class: "h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-600" %>
      </div>
      <div class="ml-3 text-sm leading-6">
        <%= label_tag :add_to_reading_list, "Add to reading list", class: "font-medium text-gray-900" %>
        <p class="text-gray-500">Automatically queue this book and schedule it in your pipeline.</p>
      </div>
    </div>
  <% end %>

  <div class="flex justify-end gap-3">
    <%= link_to "Cancel", books_path, class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
    <%= form.submit class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 cursor-pointer" %>
  </div>
<% end %>
